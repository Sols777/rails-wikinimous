<h1>My Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title , article_path(article) %><%= link_to "🗑️" , article_path(article),data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} , class:"btn btn-danger" %></li>
  <% end %>
</ul>


<%=  link_to "+add a new article"  , new_article_path , class:"btn btn-primary"%>
